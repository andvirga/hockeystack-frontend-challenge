import React from "react";

export interface IPureTableProps {}

const PureTable = (props: IPureTableProps): JSX.Element => {
  return <div>This is the Pure Table without 3rd parties implementation</div>;
};

export default PureTable;
